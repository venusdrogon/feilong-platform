<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:aop="http://www.springframework.org/schema/aop" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-2.0.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

	<util:properties id="p_alipayPayment" location="classpath:config/alipay-payment.properties"></util:properties>
	<util:properties id="p_CMBPayment" location="classpath:config/cmb-payment.properties"></util:properties>

	<util:map id="paymentAdaptorMap">
		<entry key="3" value-ref="alipayNetPayAdaptor"></entry>
		<entry key="6" value-ref="alipayPayAdaptor"></entry>
		<entry key="14" value-ref="alipayCreditCardPayAdaptor"></entry>
	</util:map>
	
 
	<!-- alipay 支付 -->
	<bean id="alipayPayAdaptor" class="com.feilong.netpay.adaptor.AlipayPayAdaptor">

		<!-- 签名用的必须配置的参数 -->
		<property name="partner" value="#{p_alipayPayment['partner']}" />
		<property name="_input_charset" value="#{p_alipayPayment['_input_charset']}" />

		<!-- 其他需要被签名的 属性 sign、sign_type 不要配置在里面 -->
		<property name="signMap">
			<map>
				<!-- #接口 名称 不可为空 -->
				<entry key="service" value="#{p_alipayPayment['service']}"></entry>
				<entry key="seller_email" value="#{p_alipayPayment['seller_email']}"></entry>
				<entry key="subject" value="#{p_alipayPayment['subject']}"></entry>
				<entry key="body" value="#{p_alipayPayment['body']}"></entry>
				<!-- 支付类型 取值范围请参见附录“11.5 收款 类型”。 -->
				<entry key="payment_type" value="#{p_alipayPayment['payment_type']}"></entry>
				<!-- 默认支付 方式 -->
				<entry key="paymethod" value="#{p_alipayPayment['paymethod']}"></entry>
 

				<!-- 不支持这个参数 -->
				<!-- <entry key="default_login" value="#{p_alipayPayment['default_login']}"></entry> -->
			</map>
		</property>

		<!--**************************** 不属于签名用的属性 ***************************** -->
		<property name="key" value="#{p_alipayPayment['key']}" />
		<property name="gateway" value="#{p_alipayPayment['gateway']}" />
		<property name="notify_verify_url" value="#{p_alipayPayment['notify_verify_url']}" />
	</bean>

	<!-- alipay 信用卡支付 -->
	<bean id="alipayCreditCardPayAdaptor" class="com.feilong.netpay.adaptor.AlipayCreditCardPayAdaptor">

		<!-- 签名用的必须配置的参数 -->
		<property name="partner" value="#{p_alipayPayment['partner']}" />
		<property name="_input_charset" value="#{p_alipayPayment['_input_charset']}" />

		<!-- 其他需要被签名的 属性 sign、sign_type 不要配置在里面 -->
		<property name="signMap">
			<map>
				<!-- #接口 名称 不可为空 -->
				<entry key="service" value="#{p_alipayPayment['service']}"></entry>
				<entry key="seller_email" value="#{p_alipayPayment['seller_email']}"></entry>
				<entry key="subject" value="#{p_alipayPayment['subject']}"></entry>
				<entry key="body" value="#{p_alipayPayment['body']}"></entry>
				<!-- 支付类型 取值范围请参见附录“11.5 收款 类型”。 -->
				<entry key="payment_type" value="#{p_alipayPayment['payment_type']}"></entry>
				<!-- 默认支付 方式 -->
				<!-- <entry key="paymethod" value="#{p_alipayPayment['paymethod']}"></entry> -->

				<!-- 多的参数 -->
				<entry key="default_login" value="#{p_alipayPayment['AlipayCreditCardPay.default_login']}"></entry>
				<entry key="paymethod" value="#{p_alipayPayment['AlipayCreditCardPay.paymethod']}"></entry>
			</map>
		</property>

		<!--**************************** 不属于签名用的属性 ***************************** -->
		<property name="key" value="#{p_alipayPayment['key']}" />
		<property name="gateway" value="#{p_alipayPayment['gateway']}" />
		<property name="notify_verify_url" value="#{p_alipayPayment['notify_verify_url']}" />
	</bean>

	<!-- alipay 网银在线 -->
	<bean id="alipayNetPayAdaptor" class="com.feilong.netpay.adaptor.AlipayNetPayAdaptor">

		<!-- 签名用的必须配置的参数 -->
		<property name="partner" value="#{p_alipayPayment['partner']}" />
		<property name="_input_charset" value="#{p_alipayPayment['_input_charset']}" />

		<!-- 其他需要被签名的 属性 sign、sign_type 不要配置在里面 -->
		<property name="signMap">
			<map>
				<!-- #接口 名称 不可为空 -->
				<entry key="service" value="#{p_alipayPayment['service']}"></entry>
				<entry key="seller_email" value="#{p_alipayPayment['seller_email']}"></entry>
				<entry key="subject" value="#{p_alipayPayment['subject']}"></entry>
				<entry key="body" value="#{p_alipayPayment['body']}"></entry>
				<!-- 支付类型 取值范围请参见附录“11.5 收款 类型”。 -->
				<entry key="payment_type" value="#{p_alipayPayment['payment_type']}"></entry>
				<!-- 默认支付 方式 -->
				<entry key="paymethod" value="#{p_alipayPayment['paymethod']}"></entry>

				<!-- 多的参数 -->
			</map>
		</property>

		<!--**************************** 不属于签名用的属性 ***************************** -->
		<property name="key" value="#{p_alipayPayment['key']}" />
		<property name="gateway" value="#{p_alipayPayment['gateway']}" />
		<property name="notify_verify_url" value="#{p_alipayPayment['notify_verify_url']}" />
	</bean>
	
	
	
	
	 
</beans>